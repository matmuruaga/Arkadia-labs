const AnimatedLines = () => (
    <div className="absolute inset-0 pointer-events-none">
      <div className="w-full h-full bg-gradient-to-r from-transparent via-[var(--accent)]/10 to-transparent animate-pulse" />
    </div>
  );
  
  export default AnimatedLines;
  